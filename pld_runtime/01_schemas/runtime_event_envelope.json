{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "$id": "https://example.com/schemas/runtime_event_envelope.schema.json",
  "title": "PLD Runtime Event Envelope",
  "version": "2.0",
  "description": "A lightweight transport envelope for delivering PLD events with runtime context. The nested PLD event object remains the source of truth for lifecycle semantics.",

  "type": "object",

  "required": [
    "envelope_id",
    "timestamp",
    "version",
    "session",
    "runtime",
    "event"
  ],

  "properties": {

    "envelope_id": {
      "type": "string",
      "description": "Unique identifier for this envelope instance (UUIDv4 recommended). Represents a single transport instance, not the semantic event identity."
    },

    "timestamp": {
      "type": "string",
      "format": "date-time",
      "description": "Envelope creation timestamp (ISO 8601). May differ from event.timestamp."
    },

    "version": {
      "type": "string",
      "enum": ["2.0"],
      "description": "Envelope schema version (distinct from PLD event schema_version)."
    },

    "session": {
      "type": "object",
      "required": ["session_id"],
      "description": "Logical session metadata. Must align with the PLD event.",
      "properties": {
        "session_id": {
          "type": "string",
          "description": "MUST equal event.session_id."
        },
        "user_id": {
          "type": "string",
          "description": "Optional user or external system identifier."
        },
        "platform": {
          "type": "string",
          "enum": [
            "assistants_api",
            "langgraph",
            "vertex_ai",
            "rasa",
            "batch_eval",
            "custom",
            "unknown"
          ],
          "default": "unknown",
          "description": "Canonical platform type. Use `custom` when not listed."
        },
        "platform_detail": {
          "type": "string",
          "description": "Free-form detail such as internal system name, model version, etc."
        }
      },
      "additionalProperties": true
    },

    "trace": {
      "type": "object",
      "description": "Optional trace metadata for debugging and observability. NOT authoritative for PLD lifecycle ordering.",
      "properties": {
        "trace_id": {
          "type": "string",
          "description": "Logical identifier for related runtime steps or distributed trace."
        },
        "turn_index": {
          "type": "integer",
          "minimum": 0,
          "description": "Debugging-only ordering helper. NOT authoritative—use event.turn_sequence for lifecycle ordering."
        },
        "parent_trace_id": {
          "type": "string",
          "description": "Optional parent identifier used for forks or conversation branches."
        }
      },
      "additionalProperties": true
    },

    "runtime": {
      "type": "object",
      "required": ["environment"],
      "description": "Runtime execution context. May contain experimental fields.",
      "properties": {
        "environment": {
          "type": "string",
          "enum": [
            "production",
            "staging",
            "sandbox",
            "local"
          ],
          "description": "Where the runtime executed."
        },
        "mode": {
          "type": "string",
          "enum": [
            "stream",
            "batch",
            "debug",
            "audit"
          ],
          "default": "stream",
          "description": "Execution/logging intention."
        },
        "tags": {
          "type": "object",
          "description": "Optional metadata used for grouping (tenant, region, experiment, shard, etc.).",
          "additionalProperties": true
        }
      },
      "additionalProperties": true
    },

    "event": {
      "$ref": "../../docs/schemas/pld_event.schema.json",
      "x-note": "Adjust the $ref path depending on your folder layout or replace with a URI registry reference.",
      "description": "Nested PLD event. MUST be validated using the PLD event specification."
    }
  },

  "additionalProperties": false,

  "x-validation": {
    "session_id_consistency": {
      "rule": "session.session_id == event.session_id",
      "description": "Envelope MUST reference the same session as the contained event.",
      "note": "This is NOT automatically enforced by JSON Schema. Implementations MUST enforce this check in the ingestion/validation layer."
    }
  }
}
